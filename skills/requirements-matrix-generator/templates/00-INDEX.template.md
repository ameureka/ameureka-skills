<!-- Template Version: 2.0 (2026-07-06) — requirements-matrix-generator -->

# {项目/模块名} 开发文档矩阵索引

## Document Info

| Field | Value |
|-------|-------|
| Version | 1.0 |
| Status | Draft |
| Last Updated | {YYYY-MM-DD} |
| Total Layers | {N} |
| Total Documents | {N} |
| Source | {差距分析文档名} |

> **用途**: 本文档是 {模块名} 开发的总入口。
> AI 开发时应首先阅读此索引，根据任务类型定位到相应层级文档。
>
> **Status 固定词汇**（禁止自创变体）：
> `Draft → Ready-for-Specs → Specs-Generated → Implemented → Gray-Verified → Closed`
> 旁路状态：`Frozen (tripwire)`（休眠 + 数据触发条件 + 测量探针）/ `Blocked (decision)`（等待决策台账拍板）。

---

## 项目约束清单（权威单点）

> 本表是全矩阵**唯一**权威约束表；各需求文档只引用本表并登记差异项（新增/豁免/收紧），禁止整表复制造成多份漂移。

| 约束类型 | 规范要求 | 适用层级 |
|---------|---------|---------|
| {约束1} | {要求} | {层级} |
| {约束2} | {要求} | {层级} |

---

## 决策台账

| 决策 ID | 问题 | 选项 | 状态 | 拍板值 + 日期 + 理由 |
|---------|------|------|------|---------------------|
| D-001 | {待拍板问题} | {方案 A / B / C} | open / decided | {拍板后回填} |

> - 被阻塞需求在其文档「阻塞决策」表登记决策 ID，需求写成条件组（方案 A/B/C 各一组）；拍板后 Status 转出、拍板记录回填、未选分支保留存档
> - 无法当期验证/休眠的开放项用 tripwire 冻结收口：数据触发条件 + 测量探针，而非模糊时间欠账
> - 机制删除类需求必须附「盲区决策清单」（删除会连带做出哪些产品/运营决策，逐项拍板）

---

## 共享文件热点表

| 文件 | 触碰它的需求 ID | 冲突处理建议 |
|------|-----------------|-------------|
| {src/path/to/file} | {UI-001, BL-002} | {合并者模式 / 分波错开} |

> 实施期热点文件用**合并者模式**：实施 agents 只申报结构化变更（set/delete + 值），单一 consolidator 统一应用+校验；并行 agent 按文件不相交分组。

---

## 波次实施计划

| 波次 | 优先级 | 需求 ID | 回归门 | 备注 |
|------|--------|---------|--------|------|
| Wave 1 | P0 | {ID, ID} | tsc + 全量测试 + 生产 build | {structure-first 标记} |
| Wave 2 | P1 | {ID} | tsc + 全量测试 + 生产 build | |
| Wave 3 | P2/P3 | {ID} | tsc + 全量测试 + 生产 build | |

> - 每波回归门绿后才 commit
> - **波间前提复核**：实施下一波前，对各 spec 的 file:line 锚点/key 名/数量断言按当前 HEAD 重验，优先核热点表中被早波触碰的文件
> - structure-first 需求标注：「{ID} 落地后下游锚点须重验」

---

## 文档矩阵概览

```
┌─────────────────────────────────────────────────────────┐
│              {模块名} 开发文档矩阵 ({N}层{M}文档)          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Layer 0: 索引层        → 00-INDEX.md (本文档)            │
│                                                          │
│  {按实际使用的层级列出}                                    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 按任务类型快速定位

### {任务类型1}: {描述}

| 步骤 | 文档 | 内容 |
|------|------|------|
| 1 | [{编号}-{标题}](./{路径}) | {说明} |
| 2 | [{编号}-{标题}](./{路径}) | {说明} |

---

## 文档清单

### {层级编号}-{类别}

| 编号 | 标题 | 优先级 | 描述 |
|------|------|--------|------|
| {前缀}-{序号} | [{标题}](./{路径}) | P{X} | {一句话描述} |

---

## 开发优先级矩阵

```
P0（阻断）→ 立即处理
├── {编号}: {标题}
└── {编号}: {标题}

P1（高优）→ 本迭代完成
├── {编号}: {标题}
└── {编号}: {标题}

P2（功能缺口）→ 下迭代
├── {编号}: {标题}
└── {编号}: {标题}

P3（打磨）→ 有空再做
└── {编号}: {标题}
```

---

## 空层不填充理由

| 未使用层级 | 理由 |
|-----------|------|
| {NN-类别} | {确认无问题 / 未审计到（列入后续审计）} |

---

## 跨切面实施告警

| 需求 ID | 跨越层级/文件 | 告警 |
|---------|--------------|------|
| {ID} | {如 40-frontend + 70-code-standards / messages/*.json} | {不可按单文档孤立实施的原因} |

---

## AI 开发工作流

```
1. 接收任务 → 查阅本索引
2. 定位相关层级文档
3. 阅读业务逻辑（10-business-logic）
4. 查看 API 规范（20-api-specs）
5. 参考代码规范（70-code-standards）
6. 安全检查（60-security）
7. 开始实现
```

---

## 实施进度 / 收口表

| 需求 ID | Status | Specs 目录 | 实施 commit | 收口记录 | 验证证据 |
|---------|--------|-----------|-------------|---------|---------|
| {ID} | Draft | {requirements-specs/...} | {hash} | {03-tasks.md 尾节链接} | {命令 + 关键输出} |

> 铁律：**无收口记录 = 未收口**。批量实施时每个 spec 都要写收口记录（含早波）；Status 推进以本表为准，禁止只在对话里宣布完成。

---

## 下游 Specs 生成

对每个需求文档调用 `/ameureka-spec-dev` 生成 4 文件 Kiro Specs（01-04；矩阵模式下 00-discovery 由需求文档替代，默认跳过）：

| 序号 | 需求文档 | Specs 输出目录 |
|------|---------|---------------|
| 1 | {前缀}-{序号}-{标题}.md | requirements-specs/{层级}-{类别}/{前缀}-{序号}-{slug}/ |
