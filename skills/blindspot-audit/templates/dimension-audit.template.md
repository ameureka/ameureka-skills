# 维度审计文档模板（{NN}-{维度中文名}.md）

> 结构蒸馏自 007 的 01-营销准确性审计 与 009 的 02-支付回调与积分交付审计。
> 六章缺一不可；尤其 ④ 健康面（最常被省略、却是防过度工程的关键）。
> 文档最前面的 findings-summary 注释块是**救援提取点**（agent 撞限额丢返回值时主线程从这里恢复）——先写块再写正文，每完成一条发现回填一行。

---

<!-- findings-summary
{PREFIX}-1|{P0-P3}|{live-traffic/dormant/ops-only}|{标题一句话}
decisions: D-{PREFIX}-1 {标题}
-->

# {NN} - {维度中文名}审计（{PREFIX}-）

> 日期：{YYYY-MM-DD}
> 审计范围：{一句话}
> 方法：runtime > canon。全部结论锚定当前代码 file:line（HEAD={sha}，{日期}实测）+ {运行环境}实测双证；数量断言均实际执行 grep/读码取得；老文档断言经代码对抗后才引用
> 对照基线：{基线文档 or 无}
> 审计者：维度 {NN} 子代理（{专项名} blindspot-audit 编排）

---

## ① 结论摘要（≤10 行）

{健康面一段：比预期健康的地方，点名机制}
{问题面一段：最重的 2-4 个发现，点名 ID 和一句话根因}

---

## ② 现状地图（关键 file:line 清单）

| 环节 | 位置 | 要点 |
|---|---|---|
| {链路环节} | `{file}:{line}` | {机制一句话，含关键行为} |

（数字/承诺类维度可改用「真相源分层表」：营销面数字 → 真相源(runtime) → 消费方式(live/静态镜像/硬编码)）

---

## ③ 发现明细

### {PREFIX}-1 【{P0-P3}】{标题（一句话说清缺陷）}

- **严重度**：{P0-P3}（{定级理由}）
- **暴露度**：{live-traffic / dormant / ops-only}（{判定依据}）
- **证据**（{日期}实测）：
  - {file:line + 机制描述}
  - {运行环境实测结果：curl 输出摘要 / DB 查询结果}
  - {触发路径/竞态时序，若适用}
- **建议方向**：{可选方案，有多个则 (a)(b)(c) 列出}
- {若依赖用户独有知识：**待用户确认**：{需要确认什么}}
- {若 dormant：**建议 Frozen(tripwire)**：触发器 = {解冻条件}}

（逐条重复；P2/P3 可适度精简但证据锚点不可省）

---

## ④ 健康面正向核销（防止后续审计重复怀疑，均 {日期} 实测）

1. **{核销点}**：{锚点 + 一句话为什么干净}
2. ...

---

## ⑤ 待拍板决策候选（供 00 总览决策台账收编）

| # | 决策点 | 关联发现 | 选项 |
|---|---|---|---|
| D-{PREFIX}-1 | {问题} | {PREFIX}-N | (a) {方案}；(b) {方案}；(c) {方案}（推荐 {x}） |

---

## ⑥ 与其他维度的交叉引用

- **→ {0X} 维（{维度名}）**：{本维发现对该维的输入/裁定/边界说明}
- **背景事实修正**：{审计中发现任务书/上游文档的错误陈述，显式纠正}

---

*产物：本文件 = 维度 {NN} 落盘审计文档。结构化 findings 已同步返回主线程。*
