# gpt-imageflow 中转配置模板。复制为 .env 并填入真实值。
# 配置优先级：环境变量 > .env 文件（--env-file 可指定路径）。
# ⚠️ 不要提交真实 key。

GPT_RELAY_BASE_URL=https://your-relay.com/v1
GPT_RELAY_API_KEY=sk-xxxx
# GPT_RELAY_MODEL=gpt-image-2          # probe / 默认模型
# GPT_IMAGE_USER_AGENT=my-project/1.0  # 自报家门，会过中转前的 Cloudflare